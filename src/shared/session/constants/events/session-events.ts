/**
 * Session state events
 */
export const SESSION_EVENTS = {
    IDLE: "session.idle",
    BUSY: "session.busy",
    ERROR: "session.error",
    DELETED: "session.deleted",
    CREATED: "session.created",
} as const;
