/**
 * Task Metadata Keys
 */

export const TASK_METADATA = {
    PARALLEL: "parallel",
    PARALLEL_GROUP: "parallel_group",
    DEPENDS: "depends",
    AGENT: "agent",
    SIZE: "size",
} as const;
