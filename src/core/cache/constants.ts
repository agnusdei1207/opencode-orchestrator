/**
 * Document Cache Constants
 * 
 * @deprecated Use CACHE from shared/constants instead
 */

import { PATHS, CACHE } from "../../shared/index.js";

export const CACHE_DIR = PATHS.DOCS;
export const METADATA_FILE = PATHS.DOC_METADATA;
export const DEFAULT_TTL_MS = CACHE.DEFAULT_TTL_MS;

