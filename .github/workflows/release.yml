name: Build & Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            binary: orchestrator-linux-x64
          - os: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            binary: orchestrator-linux-arm64
          - os: macos-latest
            target: x86_64-apple-darwin
            binary: orchestrator-macos-x64
          - os: macos-latest
            target: aarch64-apple-darwin
            binary: orchestrator-macos-arm64
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            binary: orchestrator-windows-x64.exe

    steps:
      - uses: actions/checkout@v4
      
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}
          
      - name: Install cross-compilation tools (Linux ARM64)
        if: matrix.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc

      - name: Rename binary (Unix)
        if: runner.os != 'Windows'
        run: |
          cp target/${{ matrix.target }}/release/orchestrator ${{ matrix.binary }}
          chmod +x ${{ matrix.binary }}

      - name: Rename binary (Windows)
        if: runner.os == 'Windows'
        run: |
          cp target/${{ matrix.target }}/release/orchestrator.exe ${{ matrix.binary }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary }}
          path: ${{ matrix.binary }}

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: binaries

      - name: Display structure of downloaded files
        run: ls -R binaries/

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: binaries/**/*
          body: |
            # 🚀 OpenCode Orchestrator ${{ github.ref_name }}
            
            ---
            
            ## ⭐ Core Philosophy
            
            > **Humanity lands on an unknown planet. No maps. No guides. Just a mission.**
            
            You don't assume oxygen. You don't guess gravity. You **explore**, **learn**, **adapt**, and **act**.
            
            ```
            ┌───────────────────────────────────────────────────────────┐
            │  🔍 EXPLORE   →   📝 LEARN   →   🔄 ADAPT   →   ⚡ ACT        │
            │  Scan the         Document        Adjust to       Execute the │
            │  unknown          findings        the project     task        │
            └───────────────────────────────────────────────────────────┘
            ```
            
            ---
            
            ## 👨‍🚀 The Crew
            
            | Agent | Role | Mission |
            |:------|:-----|:--------|
            | 🎯 **Commander** | Orchestrator | Leads the task, delegates work to other agents, ensures completion |
            | 📋 **Planner** | Researcher | Analyzes the project, creates the plan, documents findings |
            | 🔨 **Worker** | Implementer | Writes code, modifies files, follows the project's patterns |
            | ✅ **Reviewer** | Verifier | Tests changes, checks for errors, confirms quality |
            
            ### 🌟 Crew Principles
            
            | Principle | In Practice |
            |:----------|:------------|
            | 🔍 **Never Assume** | Always check the actual code and config files first |
            | 📝 **Document Everything** | Record patterns and findings for future tasks |
            | 📚 **Verify with Evidence** | Run tests, cite sources, complete with proof |
            | 🔄 **Adapt to the Project** | Match the project's existing style and patterns |
            | 🤝 **Specialized Roles** | Each agent has a clear, focused responsibility |
            
            ---
            
            ## ✨ Key Features
            
            | Feature | Description |
            |:--------|:------------|
            | 🦀 **Rust Core** | High-performance tool engine for search, AST, and diff |
            | ⚡ **50 Parallel Tasks** | Run up to 50 agents simultaneously in isolated contexts |
            | 🔥 **Multi-File Ops** | Writes code across different files concurrently |
            | 🛡️ **Self-Healing** | Automatic recovery with intelligent retry logic |
            | 🧬 **Adaptive AI** | Agents learn project-specific patterns and adapt behavior |
            
            ---
            
            ## 📦 Installation
            
            ```bash
            npm install -g opencode-orchestrator
            ```
            
            ---
            
          generate_release_notes: true
          fail_on_unmatched_files: false

      - name: Publish to GitHub Packages
        uses: actions/setup-node@v4
        with:
            node-version: '22'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@agnusdei1207'

      - name: Install Bun
        uses: oven-sh/setup-bun@v1
        with:
          bun-version: latest
          registry-url: 'https://npm.pkg.github.com'
          scope: '@agnusdei1207'
          
      - name: Install dependencies
        run: npm install --force
        
      - name: Prepare binaries for NPM
        run: |
          mkdir -p bin
          cp binaries/orchestrator-linux-x64/orchestrator-linux-x64 bin/
          cp binaries/orchestrator-linux-arm64/orchestrator-linux-arm64 bin/
          cp binaries/orchestrator-macos-x64/orchestrator-macos-x64 bin/
          cp binaries/orchestrator-macos-arm64/orchestrator-macos-arm64 bin/
          cp binaries/orchestrator-windows-x64.exe/orchestrator-windows-x64.exe bin/
          chmod +x bin/*

      - name: Publish to GitHub Registry
        run: |
          node -e "let pkg=require('./package.json'); pkg.name='@agnusdei1207/opencode-orchestrator'; pkg.publishConfig={registry:'https://npm.pkg.github.com'}; require('fs').writeFileSync('package.json', JSON.stringify(pkg, null, 2));"
          npm publish
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
